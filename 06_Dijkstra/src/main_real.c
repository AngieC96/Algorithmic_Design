#include <stdio.h>

#include "utilities.h"


int main_real(int argc, char *argv[])
{
    Graph *H = (binheap_type *) malloc(sizeof(binheap_type));

    //Try Dijkstra with arrays and print results
    

    // Try Dijkstra with heaps and print results

    return 0;
}
